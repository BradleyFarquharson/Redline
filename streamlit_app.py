"""
Root‑level entry point for Streamlit Cloud.
Keeps the real code inside the redline package.
"""
import redline.app

if __name__ == "__main__":
    # No main() in redline.app - the module itself runs the app when imported
    pass 